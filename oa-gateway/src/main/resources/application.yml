server:
  port: 10002
  address: 0.0.0.0

spring:
  application:
    name: oa-gateway
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration
  cloud:
    nacos:
      server-addr: ${NACOS_SERVER_ADDR:localhost:8848}
      username: ${NACOS_USERNAME:nacos}
      password: ${NACOS_PASSWORD:nacos}
      config:
        enabled: true
        file-extension: yml
      discovery:
        enabled: true
    gateway:
      server:
              webflux:
                globalcors:
                  add-to-simple-url-handler-mapping: true
                  cors-configurations:
                    '[/**]':
                      allowedOriginPatterns:
                        - "*"
                      allowedMethods: [GET, POST, PUT, DELETE, OPTIONS]
                      allowedHeaders: ['*']
                      exposedHeaders: [X-Trace-Id]
                      allowCredentials: true
                      maxAge: 3600
                routes:
                  - id: identity-service
                    uri: http://localhost:8851
                    predicates:
                      - Path=/api/auth/**,/api/identity/**,/api/employees/**,/api/departments/**,/api/leaves/**
                  - id: oa-attendance-service
                    uri: http://localhost:8849
                    predicates:
                      - Path=/api/attendance/**
                  - id: oa-content-service
                    uri: http://localhost:8853
                    predicates:
                      - Path=/api/contents/**,/api/ai/**,/api/search/**
                  - id: intelligence-service
                    uri: http://localhost:8852
                    predicates:
                      - Path=/api/search/**
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:../.env[.properties]
      - "optional:nacos:${spring.application.name}.yml"
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}
      timeout: 2s


oa:
  security:
    jwt-secret: ${JWT_SECRET}
    public-paths:
      - /api/auth/login
      - /actuator/health

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  file:
    name: logs/oa-gateway.log
