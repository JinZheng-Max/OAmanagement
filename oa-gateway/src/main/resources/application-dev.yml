spring:
  # Nacos 已启用，使用注册中心服务发现
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}

oa:
  security:
    jwt-secret: ${JWT_SECRET:daimeiyong-daozhang-cancandao-zzdq-zqmeisuzhi-huaienfuzhe}
